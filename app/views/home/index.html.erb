<div class="max-w-4xl mx-auto text-center">
  <h1 class="font-bold text-5xl mb-6 text-gray-800">MCGM.in</h1>
  <p class="text-xl mb-8 text-gray-600">Mumbai Municipal Corporation - Citizen Engagement Platform</p>
  
  <div class="mb-12">
    <% if user_signed_in? %>
      <div class="bg-green-50 border border-green-200 rounded-lg p-6 mb-6">
        <p class="text-lg mb-4 text-green-800">Welcome back, <%= current_user.name || current_user.email %>!</p>
        <div class="space-x-4">
          <%= link_to "View Ward Boundaries", prabhags_path, 
                      class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-3 px-6 rounded-lg inline-block" %>
        </div>
      </div>
      
      <% if current_user.current_assignments.any? %>
        <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-6 mb-6">
          <h3 class="text-xl font-semibold text-yellow-800 mb-4">Your Current Assignments</h3>
          <div class="space-y-3">
            <% current_user.current_assignments.each do |prabhag| %>
              <div class="flex justify-between items-center bg-white p-3 rounded">
                <span class="text-yellow-700 font-medium">Prabhag <%= prabhag.number %> (Ward <%= prabhag.ward_code %>)</span>
                <div class="space-x-2">
                  <%= link_to "View", prabhag_path(prabhag), 
                              class: "bg-blue-500 hover:bg-blue-600 text-white px-3 py-1 rounded text-sm" %>
                  <% if prabhag.status == 'assigned' %>
                    <%= link_to "Trace", trace_prabhag_path(prabhag), 
                                class: "bg-green-500 hover:bg-green-600 text-white px-3 py-1 rounded text-sm" %>
                  <% else %>
                    <span class="text-orange-600 px-3 py-1 bg-orange-100 rounded text-sm">Submitted for Review</span>
                  <% end %>
                </div>
              </div>
            <% end %>
          </div>
        </div>
      <% end %>
    <% else %>
      <div class="bg-blue-50 border border-blue-200 rounded-lg p-6 mb-6">
        <h2 class="text-2xl font-semibold mb-4 text-blue-800">Join the Community</h2>
        <p class="text-blue-600 mb-6">Sign in to start contributing to Mumbai's digital ward mapping project</p>
        <div class="space-y-4">
          <%= link_to "Sign in with Google", user_google_oauth2_omniauth_authorize_path, 
                      method: :post, 
                      class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-3 px-6 rounded-lg inline-block w-full sm:w-auto" %>
          <div class="text-sm">
            <%= link_to "Sign In", new_user_session_path, 
                        class: "text-blue-600 hover:text-blue-800 underline" %>
            |
            <%= link_to "Sign Up", new_user_registration_path, 
                        class: "text-blue-600 hover:text-blue-800 underline" %>
          </div>
        </div>
      </div>
    <% end %>
  </div>
  
  <div class="grid grid-cols-1 md:grid-cols-2 gap-8 mt-16">
    <div class="bg-white p-6 rounded-lg shadow-md">
      <h2 class="text-2xl font-semibold mb-4 text-gray-800">Help Map Mumbai's Wards</h2>
      <p class="text-gray-600 mb-4">Citizens can trace ward boundaries from official MCGM PDFs to create digital maps for better civic engagement.</p>
      <%= link_to "Explore Ward Boundaries", prabhags_path, 
                  class: "bg-green-500 hover:bg-green-700 text-white font-semibold py-2 px-4 rounded" %>
    </div>
    
    <div class="bg-white p-6 rounded-lg shadow-md">
      <h2 class="text-2xl font-semibold mb-4 text-gray-800">How It Works</h2>
      <ul class="text-gray-600 space-y-2 text-left">
        <li>• Sign up and get assigned a ward boundary</li>
        <li>• Trace boundaries using official MCGM PDFs</li>
        <li>• Submit your work for community review</li>
        <li>• Help create accurate digital ward maps</li>
      </ul>
    </div>
  </div>
</div>
